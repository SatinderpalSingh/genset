<%= form_for(@tax) do |f| %>
  <% if @tax.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tax.errors.count, "error") %> prohibited this tax from being saved:</h2>

      <ul>
      <% @tax.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :taxName, "Tax Name" %><br>
    <% if @tax.taxName.blank? %>
      <%= f.text_field :taxName %>
    <% else %>
        <%= @tax.taxName %>
    <% end %>
  </div>
  <div class="field">
    <%= f.label :taxPercentage, "Tax Percentage" %><br>
    <%= f.text_field :taxPercentage %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
